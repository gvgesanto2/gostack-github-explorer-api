// import { EntityRepository, getRepository, Repository as RepositoryTypeORM } from 'typeorm';
// import Issue from '../models/issue.model';
// import Repository from '../models/repository.model';

// @EntityRepository(Repository)
// class RepositoriesRepository extends RepositoryTypeORM<Repository> {
//   public async fetchAll(): Promise<Repository | null> {
//     const issuesRepository = getRepository(Issue);

//     const issues = await

//   }
// }
